import {useFetch} from "./useFetch";
import { renderHook } from '@testing-library/react-hooks'

describe('useFetch', () => {
  // Test the useFetch hook.
  // Do not look inside of the implementation. It has a bug. Find it!
  // https://react-hooks-testing-library.com/usage/advanced-hooks
  it.skip('should return', async function () {
    // const { response } = useFetch('url', {methode:'GET'})

  });
})